# Python
__pycache__/
*.pyc
*.pyo
.ipynb_checkpoints/
.pytest_cache/

# Model weights & datasets — large binaries, do not version
checkpoints/
data/

# Large NPZ caches (embeddings) — regeneratable. Match anywhere in the tree
# (covers results/, legacy/results/, etc).
*.npz
*.npy
# Trained model checkpoints — regeneratable from caches
*.pt
*.bin
*.safetensors

# Local-only
.env
.venv/
*.log
.DS_Store
